<local:GlassWindow x:Class="CustomSearchApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:CustomSearchApp"
        Title="Custom Search" 
        Height="850" Width="750"
        Loaded="Window_Loaded">
    
    <Border CornerRadius="30" Margin="15">
        <Border.Background>
            <SolidColorBrush Color="#1E1E2D" Opacity="0.95"/>
        </Border.Background>
        
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" Opacity="0.3" ShadowDepth="0" Color="Black"/>
        </Border.Effect>
        
        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- POLA WYSZUKIWANIA -->
            <Border Grid.Row="0" 
                    CornerRadius="20" 
                    Background="#40000000" 
                    BorderThickness="1" 
                    BorderBrush="#60FFFFFF"
                    Padding="5" 
                    Margin="0,0,0,15">
                <Grid>
                    <TextBox x:Name="SearchBox" 
                             Text="Zapytaj Gemini AI..."
                             BorderThickness="0" 
                             Background="Transparent"
                             Foreground="White"
                             FontSize="18"
                             Padding="15,12"
                             VerticalContentAlignment="Center"
                             KeyDown="SearchBox_KeyDown"/>
                </Grid>
            </Border>
<StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" 
            VerticalAlignment="Center" Margin="0,0,10,0">
    <Button x:Name="VoiceButton" 
            Content="🎤" 
            Width="40" Height="40"
            Style="{StaticResource GlassButton}"
            Click="VoiceButton_Click"
            ToolTip="Włącz/wyłącz asystenta głosowego"/>
</StackPanel>

            <!-- WYNIKI -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <!-- ODPOWIEDŹ AI -->
                    <Border x:Name="AiResponseBox"
                            CornerRadius="20" 
                            Background="#30000000" 
                            BorderThickness="1" 
                            BorderBrush="#40FFFFFF"
                            Padding="20"
                            Margin="0,0,0,10"
                            Visibility="Collapsed">
                        <TextBlock x:Name="AiResponseText"
                                   Foreground="White"
                                   FontSize="16"
                                   TextWrapping="Wrap"/>
                    </Border>
                    
                    <!-- INFORMACJE -->
                    <Border CornerRadius="20" 
                            Background="#20000000" 
                            BorderThickness="1" 
                            BorderBrush="#30FFFFFF"
                            Padding="20">
                        <StackPanel>
                            <TextBlock Text="💡 PRZYKŁADOWE PYTANIA:" 
                                       Foreground="#ADD8E6"
                                       FontSize="16"
                                       FontWeight="Bold"
                                       Margin="0,0,0,10"/>
                            
                            <Button Content="Co to jest sztuczna inteligencja?" 
                                    Style="{StaticResource GlassButton}"
                                    Click="ExampleButton_Click"
                                    Tag="Co to jest sztuczna inteligencja?"
                                    Margin="0,5"/>
                            
                            <Button Content="Napisz mi krótki wiersz" 
                                    Style="{StaticResource GlassButton}"
                                    Click="ExampleButton_Click"
                                    Tag="Napisz mi krótki wiersz"
                                    Margin="0,5"/>
                            
                            <Button Content="Wyjaśnij teorię względności" 
                                    Style="{StaticResource GlassButton}"
                                    Click="ExampleButton_Click"
                                    Tag="Wyjaśnij teorię względności w prostych słowach"
                                    Margin="0,5"/>
                            
                            <Button Content="Jaka jest pogoda?" 
                                    Style="{StaticResource GlassButton}"
                                    Click="ExampleButton_Click"
                                    Tag="Jaka jest pogoda w Warszawie?"
                                    Margin="0,5"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
            
            <!-- STATUS BAR -->
            <Border Grid.Row="2" 
                    CornerRadius="10" 
                    Background="#10000000" 
                    Padding="10"
                    Margin="0,15,0,0">
                <TextBlock x:Name="StatusText"
                           Text="Gotowy - naciśnij Enter aby zapytać AI"
                           Foreground="#80FFFFFF"
                           FontSize="12"/>
            </Border>
        </Grid>
    </Border>
</local:GlassWindow>
