<local:GlassWindow x:Class="CustomSearchApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Custom Search" 
        Height="850" Width="750"
        WindowStyle="None" 
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Loaded="Window_Loaded">
    
    <!-- GŁÓWNY KONTENER Z GLASS EFEKTEM -->
    <Border CornerRadius="30" Margin="15">
        <!-- TŁO Z PRZEZROCZYSTOŚCIĄ -->
        <Border.Background>
            <SolidColorBrush Color="#1E1E2D" Opacity="0.95"/>
        </Border.Background>
        
        <!-- CIEŃ -->
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" 
                              Opacity="0.3" 
                              ShadowDepth="0"
                              Color="Black"/>
        </Border.Effect>
        
        <!-- ZAWARTOŚĆ -->
        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- POLA WYSZUKIWANIA -->
            <Border Grid.Row="0" 
                    CornerRadius="20" 
                    Background="#40000000" 
                    BorderThickness="1" 
                    BorderBrush="#60FFFFFF"
                    Padding="5" 
                    Margin="0,0,0,15">
                <Grid>
                    <TextBox x:Name="SearchBox" 
                             Text="Witaj w Custom Search!"
                             BorderThickness="0" 
                             Background="Transparent"
                             Foreground="White"
                             FontSize="18"
                             Padding="15,12"
                             VerticalContentAlignment="Center"
                             IsReadOnly="True"
                             TextAlignment="Center"/>
                </Grid>
            </Border>
            
            <!-- PANEL INFORMACYJNY -->
            <Border Grid.Row="1" 
                    CornerRadius="20" 
                    Background="#30000000" 
                    BorderThickness="1" 
                    BorderBrush="#40FFFFFF"
                    Padding="20">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="🎉 TWOJA PIERWSZA APLIKACJA!" 
                               Foreground="White" 
                               FontSize="24"
                               FontWeight="Bold"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,20"/>
                    
                    <TextBlock Text="✓ Glass efekt działa" 
                               Foreground="#90EE90"
                               FontSize="16"
                               Margin="0,5"/>
                    
                    <TextBlock Text="✓ Rozmiar: 750x850 px" 
                               Foreground="#90EE90"
                               FontSize="16"
                               Margin="0,5"/>
                    
                    <TextBlock Text="✓ Zaokrąglenia: 30px" 
                               Foreground="#90EE90"
                               FontSize="16"
                               Margin="0,5"/>
                    
                    <TextBlock Text="✓ Przezroczystość: 95%" 
                               Foreground="#90EE90"
                               FontSize="16"
                               Margin="0,5"/>
                    
                    <TextBlock Text="\nNaciśnij ESC aby zamknąć" 
                               Foreground="#ADD8E6"
                               FontSize="14"
                               HorizontalAlignment="Center"
                               Margin="0,20,0,0"/>
                </StackPanel>
            </Border>
            
            <!-- STOPKA -->
            <StackPanel Grid.Row="2" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Center"
                        Margin="0,15,0,0">
                <TextBlock Text="Custom Search Spotlight v1.0" 
                           Foreground="#80FFFFFF"
                           FontSize="12"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
