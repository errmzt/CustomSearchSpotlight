<!-- Views/MainWindow.xaml -->
<Window x:Class="CustomSearchSpotlight.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Spotlight Search" 
        Height="850" Width="750"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" Topmost="True"
        ShowInTaskbar="False" ResizeMode="NoResize">
    
    <Border CornerRadius="30" Margin="15">
        <!-- Tło z acrylic blur -->
        <Border.Background>
            <SolidColorBrush Color="#20000000" Opacity="0.8"/>
        </Border.Background>
        
        <Border.Effect>
            <BlurEffect Radius="20" KernelType="Gaussian"/>
        </Border.Effect>
        
        <!-- Główny kontener -->
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Pole wyszukiwania -->
            <Border Grid.Row="0" CornerRadius="20" Background="#40000000" 
                    BorderThickness="1" BorderBrush="#60FFFFFF"
                    Padding="5" Margin="0,0,0,15">
                <Grid>
                    <TextBox x:Name="SearchBox" 
                             Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
                             BorderThickness="0" 
                             Background="Transparent"
                             Foreground="White"
                             FontSize="18"
                             Padding="15,12"
                             VerticalContentAlignment="Center">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Enter" Command="{Binding ExecuteSearchCommand}"/>
                        </TextBox.InputBindings>
                    </TextBox>
                    
                    <TextBlock Text="Wyszukaj pliki, aplikacje, internet lub zapytaj AI..." 
                               Foreground="#80FFFFFF" 
                               Margin="20,0" VerticalAlignment="Center"
                               IsHitTestVisible="False"
                               Visibility="{Binding ShowSearchHint, Converter={StaticResource BoolToVisibilityConverter}}"/>
                </Grid>
            </Border>
            
            <!-- Wyniki -->
            <ListView Grid.Row="1" 
                      ItemsSource="{Binding SearchResults}"
                      SelectedItem="{Binding SelectedResult}"
                      Background="Transparent"
                      BorderThickness="0">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border CornerRadius="15" Background="#30000000" 
                                BorderThickness="1" BorderBrush="#40FFFFFF"
                                Margin="0,5" Padding="15">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- Ikona -->
                                <Image Source="{Binding Icon}" Width="24" Height="24"
                                       Margin="0,0,15,0"/>
                                
                                <!-- Treść -->
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Title}" 
                                               Foreground="White" 
                                               FontSize="16"
                                               FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding Description}" 
                                               Foreground="#CCFFFFFF" 
                                               FontSize="14"
                                               TextTrimming="CharacterEllipsis"
                                               MaxHeight="40"/>
                                </StackPanel>
                                
                                <!-- Typ -->
                                <Border Grid.Column="2" CornerRadius="10" 
                                        Background="#40000000" Padding="8,4">
                                    <TextBlock Text="{Binding Type}" 
                                               Foreground="#80FFFFFF" FontSize="12"/>
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
                
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListViewItem">
                                    <ContentPresenter/>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>
            
            <!-- AI Response Panel -->
            <Border Grid.Row="2" CornerRadius="20" Background="#30000000" 
                    BorderThickness="1" BorderBrush="#50FFFFFF"
                    Padding="20" Margin="0,15,0,0"
                    Visibility="{Binding ShowAiResponse, Converter={StaticResource BoolToVisibilityConverter}}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Text="🤖 AI Response" 
                               Foreground="White" FontSize="16" FontWeight="Bold"
                               Margin="0,0,0,10"/>
                    
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" MaxHeight="200">
                        <TextBlock Text="{Binding AiResponse}" 
                                   Foreground="#E0E0E0" FontSize="14"
                                   TextWrapping="Wrap"/>
                    </ScrollViewer>
                    
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                        <Button Content="🔊 Przemów" Style="{StaticResource GlassButton}"
                                Command="{Binding SpeakAiResponseCommand}" Margin="0,0,10,0"/>
                        <Button Content="📋 Kopiuj" Style="{StaticResource GlassButton}"
                                Command="{Binding CopyAiResponseCommand}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
