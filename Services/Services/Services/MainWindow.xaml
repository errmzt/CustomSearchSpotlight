<Window x:Class="CustomSearchSpotlight.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Custom Search Spotlight" Height="850" Width="750"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" Topmost="True">
    
    <Border CornerRadius="30" Background="#80000000" Margin="10">
        <Border.Effect>
            <BlurEffect Radius="20"/>
        </Border.Effect>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Pole wyszukiwania -->
            <TextBox x:Name="SearchBox" Grid.Row="0" 
                     Margin="20,20,20,10" 
                     FontSize="18"
                     Background="Transparent"
                     Foreground="White"
                     BorderThickness="0"
                     Padding="15"
                     VerticalContentAlignment="Center"
                     TextChanged="SearchBox_TextChanged"/>
            
            <!-- Wyniki -->
            <ListBox x:Name="ResultsList" Grid.Row="1"
                     Margin="20,0,20,20"
                     Background="Transparent"
                     BorderThickness="0"
                     Foreground="White">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Title}" 
                                   FontSize="16"
                                   Margin="5"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Border>
</Window>
